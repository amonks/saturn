{
  "name": "saturn",
  "version": "1.0.0",
  "dependencies": {
    "electron-debug": "^0.2.0",
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.0.1",
    "electron-prebuilt": "0.30.3"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Saturn --platform=darwin,win32 --arch=x64 --version=0.30.3 --ignore=node_modules/electron --icon=Icon.icns",
    "start": "electron ."
  }
}
